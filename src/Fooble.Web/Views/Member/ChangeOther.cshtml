@model IMemberChangeOtherViewModel

@{
    ViewBag.DisplayName = "Change Other";
}

<form method="post">
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true, string.Empty, new { @class = "text-danger" })

    <div class="form-group">
        @Html.LabelFor(model => model.NewNickname)
        @Html.EditorFor(model => model.NewNickname, new { htmlAttributes = new { @class = "form-control" } })
        @Html.ValidationMessageFor(model => model.NewNickname, String.Empty, new { @class = "text-danger" })
    </div>

    <button type="submit" class="btn btn-default">Submit</button>
</form>

<br />
<p>@Html.ActionLink("Back to Detail", "Detail", new { id = Model.Id })</p>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
